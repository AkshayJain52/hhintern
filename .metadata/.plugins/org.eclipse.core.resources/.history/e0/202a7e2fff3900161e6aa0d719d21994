package com.hhin;

import javax.annotation.Resource;

import org.springframework.beans.factory.BeanFactory;
import org.springframework.beans.factory.xml.XmlBeanFactory;
import org.springframework.core.io.ClassPathResource;

public class Test {

	public static void main(String[] arg)
	{
		Resource resource = (Resource) new ClassPathResource("applicationContext.xml");
		BeanFactory factory = (BeanFactory) new ClassPathResource("applicationContext.xml");
		BeanFactory factory2 = new XmlBeanFactory((org.springframework.core.io.Resource) resource);
		hello hello =(hello)factory.getBean("hellobean");
		hello.displayInfo();
	}
}
